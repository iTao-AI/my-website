# DRA dual demo videos plan

1. Confirm clean website checkout and create `codex/website-dra-demo-videos`.
2. Add a RED public-content contract for DRA dual demo metadata, paths, and deterministic-demo boundary.
3. Copy only the approved HR and technical 720p MP4/poster assets into `public/videos/`.
4. Extend the project data contract with optional multi-demo entries and wire DRA to the two new videos.
5. Update project card and detail rendering to expose the primary DRA demo and the technical walkthrough while preserving existing single-video projects.
6. Update README wording if needed so site media documentation does not imply all videos are generated by `npm run videos`.
7. Run focused verification: dependency install, lint, build, audit, content contract, media metadata checks, sensitive-string scans, and desktop/mobile browser QA.
8. Commit locally only. Do not push, create PR, deploy, or modify GitHub Pages settings.
